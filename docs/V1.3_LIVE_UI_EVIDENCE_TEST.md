# v1.3.0 Live Evidence Test Report

**Date:** 2026-06-25 ~00:45 UTC
**Branch:** feature/v1.3.0-evidence-trail
**Commit:** 00effb8 test: harden v1.3 evidence trail imports and verification
**PR:** https://github.com/AiM0-create/stratageo-site-suitability-portal/pull/1
**Backend URL:** http://127.0.0.1:8000 (local FastAPI uvicorn)
**Frontend URL:** http://localhost:5173 (Vite dev server)

---

## Environment Summary

| Component | Status | Notes |
|---|---|---|
| Backend server | ✅ Running | uvicorn at 127.0.0.1:8000 |
| Frontend server | ✅ Running | Vite dev at localhost:5173 |
| Chrome extension | ❌ Not connected | Browser UI testing not available |
| Local OpenAI API key | ❌ Invalid (401) | Stale env var — known "Windows OPENAI_API_KEY trap" |
| Google Places key | Not set | Expected for local dev |
| ORS key | Not set | Expected for local dev |

---

## Step 1 — Pre-flight Checks

All passed:

| Check | Result |
|---|---|
| `git log -1` | `00effb8 test: harden v1.3 evidence trail imports and verification` |
| `pytest -q` (full suite) | **335 passed, 0 failed, 0 skipped** |
| `npx tsc --noEmit` | **Clean** |
| `npm run build` | **Success** |
| Lint | Not configured (`npm run lint` → missing script) |

---

## Step 2 — Backend Health Verification ✅

```
GET /health → 200 OK
```

```json
{
  "appVersion": "1.3.0",
  "engineVersion": "1.3.0",
  "specVersion": "2.2",
  "releaseName": "Evidence Trail & Reproducible Site Selection Reports",
  "costMode": "low",
  "featureFlags": {
    "rawIntentParser": true,
    "universalArchetypes": true,
    "multiScoreOutput": true,
    "deterministicPlanning": true,
    "evidenceTrail": true
  },
  "hasOpenAIKey": true
}
```

`evidenceTrail: true` ✅ | `deterministicPlanning: true` ✅ | `appVersion: "1.3.0"` ✅

---

## Step 3 — Ruby Crossing Planning Verification ✅

Run 5× programmatically (no LLM call needed — deterministic parser + archetype registry):

| Field | v1.3.0 Result | Stable? |
|---|---|---|
| `archetypeKey` | `student_qsr_cafe` | ✅ Identical ×5 |
| `planningFingerprint` | `pfp_774efc1cee0a` | ✅ Identical ×5 |
| `planningMode` | `deterministic` | ✅ |
| Factor weights | `[32, 27, 18, 14, 9]` | ✅ |
| Factor count | 5 | ✅ |

**Fingerprint change note:** The fingerprint changed from v1.2.0's `pfp_bbe4571fe559` to `pfp_774efc1cee0a` because `ENGINE_VERSION` (an input to `planning_fingerprint()`) changed from `"1.2.0"` to `"1.3.0"`. This is **correct and expected behavior** — the fingerprint is stable within a version, and changes between engine versions. Determinism is preserved.

---

## Step 4 — API Endpoint Tests ✅

All non-LLM endpoint tests passed:

| Endpoint | Expected | Result |
|---|---|---|
| `GET /health` | 200 + v1.3.0 metadata | ✅ |
| `GET /api/v2/analyses/00000000-0000-0000-0000-000000000000/evidence` | 404 | ✅ |
| `GET /api/v2/analyses/not-a-uuid/evidence` | 400 | ✅ |
| `GET /api/v2/analyses/00000000-0000-0000-0000-000000000000/evidence.json` | 404 | ✅ |
| `GET /api/v2/analyses/00000000-0000-0000-0000-000000000000` | 404 | ✅ |

---

## Step 5 — Evidence JSON Validation ✅

Simulated `EvidenceTrail.safe_dict()` with real `QueryTracker` output:

| Check | Result |
|---|---|
| `evidenceVersion` | `"1.3.0"` ✅ |
| Secret keys in output | **NONE — CLEAN** ✅ |
| `secretFieldsRedacted` on all provider queries | `True` ✅ |
| Provider types covered | OSM Overpass · Google Places · ORS · Internal ✅ |
| JSON parseable | ✅ |
| JSON size | 3,727 bytes |
| Contains `prompt` | ✅ |
| Contains `dataSnapshot` | ✅ |
| Contains `providerQueries` | ✅ |
| Contains `studyArea` | ✅ |
| Contains `scoring` | ✅ |
| Contains `limitations` | ✅ |

---

## Step 6 — Live LLM Analysis Test ❌ BLOCKED

**Blocker:** Local `OPENAI_API_KEY` environment variable is invalid (OpenAI returns 401 Unauthorized).

This is the known ["stale Windows OPENAI_API_KEY trap"](../memory/deployment-infrastructure.md) — a local env var from a prior session is overriding the correct key. The production Cloud Run service uses keys from GCP Secret Manager and is NOT affected.

```
HTTP 401: Incorrect API key provided: sk-proj-...1hcA
```

The full analysis flow (chat turn → framework → execute → evidence trail in result) could not be completed locally.

### What this does NOT affect:
- Unit tests (335/335 pass)
- Evidence schema (validated)
- Endpoint behavior (validated)
- Planning determinism (validated ×5)
- Secret safety (validated)

### What requires manual action to verify:
- Evidence tab renders in the live UI with real analysis result
- `evidenceTrail` field appears in a real completed job result
- PDF Evidence Appendix appears in a real PDF export
- JSON export downloads from the UI

---

## Step 7 — Browser UI Test ❌ BLOCKED

Chrome extension not connected — no `list_connected_browsers()` found. Browser-driven UI testing requires the Claude in Chrome extension to be running. The frontend dev server IS running at localhost:5173 but no automated browser interaction is possible.

---

## Step 8 — Security Assessment ✅

| Check | Result |
|---|---|
| `safe_dict()` strips all secret-looking keys | ✅ Tested |
| `secretFieldsRedacted: true` on all provider queries | ✅ |
| No secret values appear in evidence JSON | ✅ |
| Evidence endpoints follow existing GET /api/v2/analyses/* auth (exempt from token gate) | ✅ Documented |
| Per-user job isolation: NOT implemented | ⚠️ Pre-existing arch design, documented |

---

## Fingerprint Reference (Updated for v1.3.0)

| Version | Prompt | `archetypeKey` | `planningFingerprint` |
|---|---|---|---|
| v1.2.0 | Ruby Crossing student QSR | `student_qsr_cafe` | `pfp_bbe4571fe559` |
| v1.3.0 | Ruby Crossing student QSR | `student_qsr_cafe` | `pfp_774efc1cee0a` |

The fingerprint is deterministic within each version. Change between versions is expected (ENGINE_VERSION is an input).

---

## Known Limitations

1. **Full live LLM test blocked** — invalid local OpenAI API key
2. **Browser UI test blocked** — Chrome extension not connected
3. **Full data replay not implemented** — audit-reproducible only (documented in V1.3_REPRODUCIBILITY_LIMITATIONS.md)
4. **Per-user job isolation not implemented** — existing architecture (documented in V1.3_TEST_HARDENING_REPORT.md)
5. **Lint not configured** — no `npm run lint` script

---

## Final Decision

### ✅ Passed
- Backend v1.3.0 starts, health returns correct metadata
- Planning determinism (×5): `student_qsr_cafe`, `pfp_774efc1cee0a`, weights `[32,27,18,14,9]`
- API endpoint structure (400/404 behavior)
- Evidence schema secret safety
- Full unit test suite (335/0)
- TypeScript, build

### ❌ Blocked (manual action required)
- Live LLM analysis → evidenceTrail in real result → Evidence tab in UI → PDF Appendix → Evidence JSON from UI

### Required before merge
1. Fix or bypass the invalid local `OPENAI_API_KEY` (use `.env` in `backend-py/` with a valid key), OR
2. Deploy v1.3.0 to a staging Cloud Run instance (the production keys in Secret Manager are valid) and run the Ruby Crossing prompt through the live portal
3. Confirm `evidenceTrail` field appears in the result JSON
4. Confirm Evidence section appears in ResultsDrawer
5. Confirm Evidence JSON export has no secrets
6. Confirm PDF includes Evidence Appendix text

---

**Status: LIVE UI TEST BLOCKED — MANUAL ACTION REQUIRED**
