# V1.3.0 Evidence Trail Verification Report

**Date:** 2026-06-25
**Branch:** feature/v1.3.0-evidence-trail
**Author:** v1.3.0 evidence trail upgrade

---

## Branch and Commit State

- Branch: `feature/v1.3.0-evidence-trail`
- Base: `master @ 8e9a96a` (v1.2.0 post-deploy smoke test)
- Status: feature branch, not yet merged

---

## Test Results (post-hardening)

### Full backend test suite: `pytest -q`
- **Total: 335 tests**
- **Passed: 335**
- **Skipped: 0**
- **Failed: 0**
- **Collection errors: 0**

### Root cause fixed (see V1.3_TEST_HARDENING_REPORT.md)
The `openai` import-chain fragility was fixed by making `AsyncOpenAI` imports lazy in `engine/results.py` and `services/critic.py`. All 5 previously erroring test files now collect and pass without requiring `openai` installed.

### Evidence trail tests: `tests/test_evidence_trail.py`
- **43 tests, all pass** (was 34 pass + 2 skip before hardening)
- Includes: import-safety tests, endpoint validation tests, PDF appendix marker test

### TypeScript
- `npx tsc --noEmit` — **CLEAN** (0 errors)

### Build
- `npm run build` — **SUCCESS** (8.84s)

---

## Evidence Schema Summary

| Schema class | Fields | Tests |
|---|---|---|
| `EvidenceTrail` | 16 top-level fields | Default construction, JSON roundtrip |
| `ProviderQueryEvidence` | 10 fields | Secrets redacted from queryParamsPublic |
| `FactorEvidence` | 11 fields | Per-candidate raw/normalized/weighted |
| `CandidateEvidence` | 14 fields | Score breakdown, exclusion reasons |
| `ExclusionEvidence` | 8 fields | Mask stats → ExclusionEvidence records |
| `ScoringEvidence` | 9 fields | Formula, weight sums, normalization |

---

## Secret Safety Verification

- `safe_dict()` runs `_scrub_secrets()` recursively on all evidence output
- Keys matching `api_key|apikey|authorization|token|secret|password|openai_api_key|google_places_api_key|ors_api_key|app_shared_token` → replaced with `"[REDACTED]"`
- Test `test_safe_dict_no_secrets`: injects `api_key = "sk-secret"` into `queryParamsPublic`, confirms it becomes `"[REDACTED]"` in `safe_dict()` output
- Test `test_query_tracker_no_secrets_in_records`: confirms no `api_key|authorization|token|bearer` strings appear in tracker records

---

## Ruby Crossing Evidence Regression

| Check | Result |
|---|---|
| `test_ruby_crossing_evidence_archetype` | `student_qsr_cafe` ✅ |
| `test_ruby_crossing_evidence_factor_count` | 5 factors ✅ |
| `test_ruby_crossing_evidence_weights_sum` | sum = 100 ✅ |
| `test_ruby_crossing_canonical_weights` | [32, 27, 18, 14, 9] ✅ |
| `test_deterministic_planner_still_works` | archetypeKey=student_qsr_cafe ✅ |
| `test_planning_fingerprint_stable` | pfp_ prefix, deterministic ✅ |

---

## API Endpoints Added

| Endpoint | Purpose |
|---|---|
| `GET /api/v2/analyses/{jobId}/evidence` | Returns evidence trail JSON |
| `GET /api/v2/analyses/{jobId}/evidence.json` | Download as `stratageo-evidence-{jobId[:8]}.json` |

Both endpoints: 400 on invalid UUID, 409 if not done, 404 if no trail, 200 + evidence on success.

---

## UI Evidence Tab

Added to `ResultsDrawer.tsx`:
- State: `showEvidence`, `evidenceExpandedFactor`
- Conditionally renders `evidenceTrail` from `(result as any).evidenceTrail`
- 7 collapsible sub-sections
- JSON export via `URL.createObjectURL` (client-side, no server call)
- Graceful fallback when `evidenceTrail` is undefined (saved/demo results)
- TypeScript: all `EvidenceTrail` types imported from `../types`

---

## Known Limitations

1. **Full data replay not implemented**: EvidenceTrail is audit-reproducible, not fully replayable. Provider data changes between runs.
2. **Per-cell exclusion detail not available**: Exclusion ledger records batch-level counts (e.g. "5 cells removed by railway"), not per-cell IDs.
3. **Constraint enforcement evidence minimal**: `ConstraintEvidence` schema deferred to v1.3.1; current version shows constraint checks from `locations.exclusions`.
4. **`openai`-dependent tests**: `test_jobs_imports_evidence_builder` and `test_build_methodology_contains_layers` skip in local env without `openai` installed. Run `pip install openai` to include them.
5. **Evidence trail absent from demo/saved results**: Demo scenarios and saved analyses loaded from Firestore don't include `evidenceTrail`. UI shows "unavailable" gracefully.

---

## Safe to Open PR?

**YES** — all checks pass:
- TypeScript clean
- Build succeeds
- 34/36 new evidence tests pass (2 expected skips)
- All 257 pre-existing tests still pass
- No secrets in committed code
- v1.2.0 deterministic planning not weakened
- No deployment to production yet

---

## Safe for Demo?

**YES** — factor schema and planning fingerprint still stable from v1.2.0. Evidence trail is additive — it does not change any existing behavior. The Evidence Trail section renders conditionally and fails gracefully if absent.

---

## Safe for Client Recommendations?

**CONDITIONAL** — same as v1.2.0. Planning is deterministic. Execution depends on live provider data. Field validation required before acting on any recommendation. The evidence trail now makes it possible for clients to inspect every step — which strengthens the audit chain.
